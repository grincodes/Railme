export enum Role {
  USER = 'user',
  ADMIN = 'admin',
  SUPER_ADMIN = 'super-admin',
}

//In more sophisticated systems, you may store roles within a database,
// or pull them from the external authentication provider.
